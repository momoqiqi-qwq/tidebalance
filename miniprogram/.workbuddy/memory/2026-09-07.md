# 2026-09-07

## 项目概览确认（Le时间管理 小程序版）
- 目录：`E:\E-Develop-Project\时间管理软件\miniprogram`，原生 WXML/WXSS/JS，零依赖零构建；appid 仍为 `touristappid`（发布前需替换）。
- 分层：`app.js/json/wxss + theme.json` → `core/{store,timeParser,captureFlow}.js` → `pages/{quadrant,timeblock,capture,settings,task}` → `wx.setStorageSync("letime-data")`。
- 共 2752 行代码；父目录还有 `le-time-management/`（Tauri 桌面端）、`tools/`（校验与测试脚本）、`ui-概念稿/`。
- 校验结果：`node tools/test-miniprogram-core.js` → 39 通过 0 失败；`node tools/check-miniprogram.js` → 静态校验全部通过。
- 关键约定：数据单 JSON（tasks/blocks/settings/plugins）与桌面端互通；时间轴 07:00–24:00、54px/小时、15 分钟吸附；iOS <16.4 禁用正则 lookbehind。
- 待办：存储 1MB/key 上限（图片附件备份可能超限）、无国际化、tabBar 图标不随主题切换。

## 搭建小程序上传通道（miniprogram-ci）
- 用户要求直接上传代码，但本机**未安装微信开发者工具**（各盘与注册表均无），且 appid 为 `touristappid`（游客号不能上传）。用户目前只有测试号，故上传未真正执行，只把通道搭好。
- 新增 `tools/upload-miniprogram.js`（仓库根 tools/）：参数/环境变量双通道（MP_APPID、MP_KEY、MP_VERSION、MP_DESC、MP_ROBOT），支持 `--check` 自检、`--write-appid` 回写 project.config.json、常见报错（IP 白名单 47001 / 密钥 / appid 不匹配）给出针对性提示。
- miniprogram-ci 2.1.31 装在隔离工作区 `C:\Users\yile\.workbuddy\binaries\node\workspace`，项目目录不留 node_modules；脚本按 `NODE_PATH` → `~/.workbuddy/...` 顺序查找。
- **坑**：miniprogram-ci 依赖的 npm-conf 在 win32 下 `path.resolve(process.env.APPDATA, ...)`，本 shell 的 APPDATA 为空会抛 `paths[0] must be of type string`。脚本里已在 require 前用 `USERPROFILE\AppData\Roaming` 兜底。
- `.gitignore` 已加 `private.key / *.upload.key / upload-keys/`；README 增加「上传发布」章节。
- 真正上传还缺：正式 appid + 公众平台下载的 private.key + 公网 IP 加入白名单。

## 用户装好微信开发者工具后的排查
- 安装路径 `E:\E-Develop-Project\微信web开发者工具`（**非**默认 Program Files），CLI 为 `./cli.bat`。
- **tasklist 中文进程名必须 `iconv -f GBK -t UTF-8` 才能 grep 到**，否则一直误判为"未运行"。
- 用户已用微信账号登录：昵称「乐乐」，`loginStatus: SUCCESS`、`isTourist: false`（在 `WeappLocalData/localstorage_bb1f1c31590074019fb12d13fe40f221.json`）。
- IDE 设置存在 `%LOCALAPPDATA%\微信开发者工具\User Data\<hash>\WeappLocalData\`，`hash_key_map_2.json` 里 `b72da75d79277d2f5f9c30c9177be57e` = `reduxPersist:settings`；**有 localstorage_ 与 ls_ 两份镜像，必须同时改**。
- 开启 CLI 服务端口 = 两份文件里 `security.enableServicePort = true`（已改，含 .bak 备份）；**改完必须重启 IDE 才生效**。注意 `Default/.cli` 里的端口号是 CLI 自己写的，不代表 IDE 服务在监听。
- **无法从本沙箱启动 IDE**：Electron 启动会调用 `reg.exe`，被安全策略（Program Blacklist）拦截。只能让用户手动打开 IDE。
- IDE 里唯一项目 `C:\Users\yile\WeChatProjects\miniprogram-1`（小程序示例模板），appid `wxa3f6a0e1c89d36dd`，疑为测试号 appid，待用户确认。
- taskkill 用法：Git Bash 下需 `export MSYS_NO_PATHCONV=1` 再 `taskkill /F /IM "微信开发者工具.exe"`，否则 `/F` 被当路径。

## ✅ 上传成功（23:24）
- 用户手动打开 IDE 后服务端口自动开启（127.0.0.1:15697），`cli islogin` 返回 `{"login":true}`。
- 上传命令：`cd "E:/E-Develop-Project/微信web开发者工具" && ./cli.bat upload --project "E:\\E-Develop-Project\\时间管理软件\\miniprogram" -v 1.0.0 -d "Le时间管理 首次提交"` → `√ upload`，包体 69.8 KB（71476 B）。
- 结论：`wxa3f6a0e1c89d36dd` 是**可上传的正式 appid**（不是测试号）；project.config.json 已改并删除 .bak。
- 后续：代码在 mp.weixin.qq.com → 管理 → 版本管理 → 开发版本，需人工提交审核；体验版要先加体验成员。
- 注意 tasklist 中文进程名必须 iconv 转码；沙箱无法启动 IDE（reg.exe 被拦），只能让用户手动开。
