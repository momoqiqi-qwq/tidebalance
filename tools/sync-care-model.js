const fs = require('node:fs');
const path = require('node:path');
const root = path.join(__dirname, '..');
const source = fs.readFileSync(path.join(root, 'le-time-management/src/careModel.js'), 'utf8');
const names = [...source.matchAll(/export function (\w+)/g)].map(m => m[1]);
fs.writeFileSync(path.join(root, 'miniprogram/core/careModel.js'), '// Generated by tools/sync-care-model.js; edit le-time-management/src/careModel.js.\n' + source.replace(/export function /g, 'function ') + '\nmodule.exports = { ' + names.join(', ') + ' };\n');
console.log('Care model synchronized');
